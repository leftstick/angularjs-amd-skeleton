/**
 *  Entrance of services
 *
 *  @author  haozuo
 *  @date    Oct 13, 2015
 *
 */
(function(define) {
    'use strict';

    define([
        './Events',
        './Utils'
    ], function() {
        return [].slice.call(arguments, 0);
    });

}(define));
